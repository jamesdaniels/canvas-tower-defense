function Tower(x, y) {
	this.x     = x;
	this.y     = y;
	this.angle = 0;
	return this;
};

Tower.prototype.draw = function(ctx) {
	// ctx.beginPath();
	// ctx.fillStyle = '#ffffff';
	// ctx.arc(this.x, this.y, 3, 0, Math.PI*2, true);
	// ctx.closePath();
	// ctx.fill();
};
